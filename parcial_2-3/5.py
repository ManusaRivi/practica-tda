def validador_hamiltonian_completition(grafo, K, ciclo):
    if len(solucion.obtener_aristas()) < K:
        return False
    
    inicio = ciclo[0]
    actual = inicio
    siguiente = ciclo[1]

    # COMPLETAR

    return False